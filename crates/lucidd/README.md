# lucidd

The LUCID inference daemon — the open GPU inference flagship built on the Phase substrate. Implements `phase-protocol::Worker` for inference jobs (llama.cpp, MLX, and future backends), depending on `phase-net`, `phase-identity`, `phase-manifest`, `phase-receipt`, and `phase-protocol` by path during the monorepo phase.
