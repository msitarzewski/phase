# phase-identity

Persistent Ed25519 keypair management for Phase nodes. Generates, loads, and saves the node's signing key to a platform-appropriate on-disk location so peer identity remains stable across daemon restarts.
