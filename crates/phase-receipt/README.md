# phase-receipt

Generic signed receipt types for Phase. Exposes `SignedReceipt<T>` so any worker can produce a cryptographically verifiable record of a completed job, regardless of the result payload shape.
