# Phase Boot - Local Mode
# Network connectivity with local caching, no external downloads

title Phase Boot — Local Mode
linux /vmlinuz-x86_64
initrd /initramfs-x86_64.img
options phase.mode=local phase.channel=stable phase.cache=enabled console=tty0 console=ttyS0,115200 earlyprintk=efi,keep loglevel=8 ignore_loglevel fbcon=map:1 video=efifb:force
